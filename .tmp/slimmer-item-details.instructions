Code Path: /home/paule/MAD
Context File: /home/paule/MAD/.mad/CONTEXT.md

You are a senior code reviewer tasked with critically evaluating an implementation.

## Feature: slimmer-item-details

### Original Plan:
## Implementation Plan: Slirmer Item Details with Collapsible Sections

### Overview
Add collapsible show/hide sections to the Item Details pane in both TUI and web UI. Title and Description are always visible. History is expanded by default. All other sections (plan, impl_spec, test_spec, impl_notes, questions) default to collapsed. Empty sections are shown but collapsed. State does not persist across sessions.

---

### Phase 1: Data Layer Changes (Web UI)

1. **Update FeatureSummary struct** in `server/hub.go:31-42`:
   - Add `Plan string `json:"plan,omitempty"``
   - Add `ImplSpec string `json:"impl_spec,omitempty"``
   - Add `TestSpec string `json:"test_spec,omitempty"``
   - Add `ImplNotes string `json:"impl_notes,omitempty"`

2. **Update pipeline server_client** to send these new fields:
   - Modify `server_client.py` to include plan, impl_spec, test_spec, impl_notes when sending feature data to the server

3. **Update client.html data attributes**:
   - Add `data-plan`, `data-impl-spec`, `data-test-spec`, `data-impl-notes` to feature card elements in the HTML template

---

### Phase 2: Web UI Implementation

1. **Modify `toggleFeatureDetail()` in `client.html:407-483`**:
   - Restructure the details pane to use collapsible sections
   - Always visible: Title, Description
   - Collapsible sections with expand/collapse toggle:
     - History (default: expanded)
     - Plan (default: collapsed)
     - Implementation Spec (impl_spec) (default: collapsed)
     - Test Spec (test_spec) (default: collapsed)
     - Implementation Notes (impl_notes) (default: collapsed)
     - Questions (default: collapsed, or inline when stage === 'requested-input')
   - Empty sections: Render section header with toggle but collapsed (show "No [section] provided" placeholder)

2. **Questions form conditional rendering**:
   - When stage === 'requested-input': Render questions inline (as-is, not in collapsible)
   - When stage !== 'requested-input': Render as collapsible section, collapsed by default

3. **Use existing CSS pattern** from stage toggles:
   - Apply `.stage-toggle.collapsed` and `.stage-body.hidden` classes
   - Ensure toggle icons rotate appropriately

4. **Accessibility**:
   - Add `aria-expanded` attributes to toggle buttons
   - Add `aria-controls` linking toggles to content sections
   - Ensure keyboard navigation (Enter/Space to toggle)
   - Add `role="region"` to collapsible content areas

5. **Handle long content**:
   - Add `max-height` and `overflow-y: auto` to collapsible content containers
   - Smooth transition on expand/collapse

---

### Phase 3: TUI Implementation

1. **Verify Textual Collapsible widget**:
   - Check that Textual version supports `Collapsible` widget (available since v0.37.0)
   - If not available, implement custom collapsible using `CollapsibleGroup` or toggle behavior

2. **Modify `_feature_markdown()` in `tui.py:100-150`**:
   - Replace flat Markdown rendering with structured layout using Widgets
   - Use Textual's `Collapsible` widget for each section

3. **TUI Section Structure**:
   - Always visible: Title, Description (rendered outside collapsibles)
   - Collapsible sections:
     - History (default: expanded)
     - Plan (default: collapsed)
     - Implementation Spec (impl_spec) (default: collapsed)
     - Test Spec (test_spec) (default: collapsed)
     - Implementation Notes (impl_notes) (default: collapsed)
     - Questions for Human (default: collapsed)
   - Exclude Pipeline Log (confirmed: only actual Item Details)

4. **Empty section handling**:
   - Show section header with collapse icon
   - Display "No [section] provided" text inside collapsed section

5. **Styling**:
   - Match existing TUI theme (colors, borders)
   - Ensure toggle indicators are visible
   - Add keyboard shortcuts for expand/collapse (optional: arrow keys)

---

### Phase 4: Testing

1. **Test empty sections**: Verify empty plan/impl_spec/test_spec/impl_notes show as collapsed with placeholder text
2. **Test default states**: Verify History expanded, others collapsed on initial load
3. **Test toggle behavior**: Verify expand/collapse works via click and keyboard
4. **Test non-persistence**: Refresh page/TTY and verify default states restored
5. **Test long content**: Verify scroll behavior in collapsed sections
6. **Test Questions conditional**: Verify inline rendering when stage === 'requested-input'

---

### Files to Modify

1. `server/hub.go` - Add fields to FeatureSummary
2. `pipeline/server_client.py` - Send new fields to server
3. `server/templates/client.html` - Web UI collapsible sections
4. `pipeline/tui.py` - TUI collapsible sections


### Test Spec (requirements):
Test specification written to `/home/paule/MAD/.mad/boards/mad/spec-writing/slimmer-item-details.test-spec.md`.

### Implementation:
Implementation complete. Here's a summary of changes made:

**Files Modified:**

1. **pipeline/tui.py:**
   - Added `Collapsible` to imports from `textual.widgets`
   - Modified compose method to use a `Vertical` container (`#detail-container`) instead of a `Markdown` widget for the detail view
   - Created new function `_build_feature_detail_widgets()` that builds collapsible sections:
     - Always visible: Title, Description
     - Collapsible (expanded by default): History
     - Collapsible (collapsed by default): Plan, Implementation Spec, Test Spec, Implementation Notes, Questions
     - Empty sections show "No [section] provided" placeholder
   - Modified `_update_detail_view()` to mount Collapsible widgets instead of updating a Markdown widget
   - Updated `action_toggle_focus()` to reference the new container ID

2. **pipeline/test_server_client.py:**
   - Updated `_make_feature()` test fixture to include the new fields (`plan`, `impl_spec`, `test_spec`, `impl_notes`) that are now sent to the server

**Verification:**
- Server builds successfully (`make server-build`)
- All 86 tests in `test_server_client.py` pass
- TUI imports successfully

The implementation follows the feature spec:
- Title and Description are always visible (outside collapsibles)
- History is expanded by default
- Plan, Impl Spec, Test Spec, Impl Notes, Questions are collapsed by default
- Empty sections show placeholder text
- State does not persist across sessions (no persistence mechanism added)

### Tests Written

Now let me examine the server hub.go to verify the FeatureSummary struct:
Now let me check how the tests are run:
Let me check if there's more context in the tui.py file:
Now I understand the code structure. The existing tests already cover much of what's testable in Python. Let me verify what's actually missing by running all tests:
The existing tests cover the Python-testable behaviors. Let me verify there are no compile or import errors:
Let me also verify the complete test suite runs:
The test failures are caused by the `_make_feature` fixture in `test_start_agent_e2e.py` missing the new fields. Let me also check `test_auto_mode_full.py`:
I need to fix the `_make_feature` fixtures in both test files to include the new fields:
All 429 tests pass. The existing test files already covered the Python-testable behaviors for the `slimmer-item-details` feature:

1. **Server client** (`test_server_client.py`): Tests that `push_state` sends the new fields (`plan`, `impl_spec`, `test_spec`, `impl_notes`) to the server

2. **TUI** (`test_slimmer_item_details_tui.py`): Tests that:
   - Title and Description are always visible
   - History is expanded by default
   - Plan, ImplSpec, TestSpec, ImplNotes, Questions are collapsed by default
   - Empty sections show placeholder text

I fixed the test failures by adding the missing fields to `_make_feature` fixtures in:
- `test_start_agent_e2e.py` 
- `test_auto_mode_full.py`

---

## Review Checklist

For each item below, explicitly assess and document your findings:

1. **Completeness**: Does the implementation cover ALL items in the plan? List any missing items.

2. **Test Coverage**: Do tests verify ALL behaviors in the test spec? List any untested behaviors.

3. **Code Quality**:
   - Are there obvious bugs (null pointer, off-by-one, race conditions)?
   - Is error handling adequate?
   - Are there security concerns (injection, auth bypass)?
   - Is there proper resource cleanup (files, connections)?

4. **Edge Cases**: What happens with:
   - Empty inputs?
   - Very large inputs?
   - Concurrent access?
   - Network failures?
   - Invalid states?

5. **Architecture**:
   - Is the code maintainable?
   - Are dependencies explicit?
   - Is there appropriate abstraction?

---

## Output Format

Respond with EXACTLY this format (no other text). Use PLAIN TEXT only - no markdown formatting:

**VERDICT: PASS** or **VERDICT: FAIL**

If FAIL, you MUST provide specific, actionable feedback. Use plain text, no bold, no code blocks, no markdown:
FEEDBACK: List each issue with file/function/line if possible, and what needs to be fixed.

Example:
```
VERDICT: FAIL
FEEDBACK:
- Plan item "add authentication" not implemented - no login/user code found
- Test spec requires "rate limiting" but no tests for it
- NullPointerException in handle_submit() line 42 if input is None
- Missing try/finally for file handle in process_data()
```

When finished, write "REVIEW_COMPLETE" on its own line and then exit. Do not wait for more input.
