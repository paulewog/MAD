{
  "summary": "Proposed a concrete implementation approach for BOARD_CONTEXT.md: (1) Create the file at .mad/boards/<board>/BOARD_CONTEXT.md, loaded alongside CONTEXT.md in runner.py's _prepend_context() method by accepting a board parameter and reading the board-specific file via a new read_board_context_file() in config.py. (2) Hook into the three 'done' transition points (tui.py:3066, tui.py:3296, server.py:475) to trigger a lightweight agent call that extracts lessons from the completed feature's JSON data (plan, impl_notes, history, review feedback) and appends a structured summary block to BOARD_CONTEXT.md. (3) Implement size management with a ~50KB soft cap — when exceeded, run a summarization agent that condenses older entries while preserving the most recent N entries verbatim, keeping the file human-readable and editable. Key design decisions to consider: whether the summary-generation agent call should block the done transition or run async; whether to use a structured format (YAML frontmatter per entry with date/feature/lessons) vs. free-form markdown; and whether BOARD_CONTEXT.md should take precedence over or supplement CONTEXT.md when both exist. Recommended keeping both — CONTEXT.md for project-wide constants, BOARD_CONTEXT.md for evolving board-specific learnings — with BOARD_CONTEXT.md inserted after CONTEXT.md in the prompt preamble."
}
