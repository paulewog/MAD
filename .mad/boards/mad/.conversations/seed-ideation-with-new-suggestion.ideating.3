{
  "summary": "Building on the second round's valid critique about prompt hierarchy and field lifecycle, the implementation should place the ideation_prompt injection after the description but before the task section in ideating.md using a simple conditional block like '{ideation_prompt_section}' that renders as '## User Direction\\n{ideation_prompt}\\nThe above user direction should guide your ideation — prioritize it over default assumptions, but still fulfill your assigned role below.' This placement avoids the hierarchy confusion the previous round identified while keeping it prominent. The field should persist after ideation completes (not auto-clear) since it serves as documentation of the user's intent, but the UI should show a small indicator (e.g., a marker in the TUI list and a badge in WebUI) when an item has a seed prompt set so users are aware. On the API side, the second round is correct that a dedicated endpoint is unnecessary — the existing generic edit flow should handle ideation_prompt as just another editable string field, which means the only backend changes are adding the field to FeatureFile in state.py and including it in the edit modal fields list in both tui.py and client_full2.html. The ideating.md template change and the phases.py run_ideating() prompt-building logic are the only non-trivial pieces: in phases.py, build the ideation_prompt_section string conditionally from feature.ideation_prompt before passing it to _build_prompt(), keeping the template itself simple and the logic testable."
}
