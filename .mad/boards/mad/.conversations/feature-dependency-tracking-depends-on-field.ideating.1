{
  "summary": "This feature requires changes across five layers: (1) Data model in state.py — add a `depends_on: list[str]` field to FeatureFile with slug-based references, a `is_blocked()` property that checks whether all dependencies are in the 'done' stage, and a `check_circular_deps()` validator using DFS cycle detection run on save/create; (2) Pipeline auto-mode in server.py and tui.py — the `_auto_run_check()` feature-selection loop should call `feature.is_blocked()` and skip blocked features just like it skips cooldown/retry-exceeded ones, keeping the filtering co-located with existing skip logic; (3) Stage-transition hooks — when `move_to_stage('done')` is called, scan all loaded features for those whose `depends_on` includes the just-completed slug, check if they're now fully unblocked, and log the unblock event to both `pipeline_log` and `history`; (4) WebUI (api.go + index.html) — expose `depends_on` and computed `blocked` boolean in the feature JSON sent to the frontend, render a blocked/unblocked badge on kanban cards, and add a dependency picker in the edit modal using a multi-select of sibling feature slugs filtered to exclude self and already-done features; (5) TUI (tui.py) — add a 'Dependencies' tab or section in the detail view showing dependency slugs with their current stage, and add an edit action for setting dependencies. Key design decisions to resolve: whether cross-board dependencies are allowed (recommend no, keep it intra-board for simplicity), whether to use slugs or IDs as the reference key (slugs are more human-readable and already used for `find()`), and whether blocked features should be visually dimmed or just badged in the kanban view (recommend both — dim card + show a lock icon)."
}
