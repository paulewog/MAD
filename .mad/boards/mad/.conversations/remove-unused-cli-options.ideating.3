{
  "summary": "Building on both prior contributions, here is a refined removal plan with specific secondary cleanup items. The `Markdown` import (line 25) becomes unused after removing the `status` command (the only consumer at line 783) and should be deleted. The `Table` import stays — it's used by `schedule list` (line 1140) and `runs list` (line 1324), even though the `agent list` consumer (line 839) is being removed. The `get_runner()` helper (lines 111-112) is never called by any kept command (all kept commands inline `AgentRunner(config)`) and should be removed alongside `_read_input`, `FIELD_SETTERS`, and `FIELD_GETTERS`. For `find_feature_or_exit` (kept, used by `run`, `restart`, schedule, and runs commands): the hint on line 120 should change from \"Try 'pipeline ls'\" to \"Try the TUI or web UI to list features.\" Similarly, line 389's reference to \"pipeline approve <feature>\" should be rewritten since `approve` never existed as a command — change to \"Use the TUI or web UI to approve features first.\" The stale module docstring (lines 1-16) should be rewritten to list only the kept commands: `run`, `restart`, `auto`, `tui`, `lock`, `service`, `schedule`, and `runs`. For test cleanup, `test_edit_feature.py` should be deleted entirely. One additional consideration the prior critiques missed: the `review` command's removal also removes the only call site for `runner.interactive()` from pipeline.py — verify that `interactive()` isn't dead code in `runner.py` itself (it may still be used by the TUI). The overall removal scope is ~450 lines including docstring rewrites and secondary cleanup, reducing pipeline.py from ~1387 lines to ~930 lines."
}
