{
  "summary": "Both prior analyses correctly converge on the root cause: the initial '/' handler (api.go:252-258) omits 'ActiveSlugs' from the template data, while the board_fragment template (index.html:921) calls '{{if index $.ActiveSlugs .Slug}}' which panics on a nil map in Go's template engine. The fix is simple and surgical: add 'ActiveSlugs' computation to the initial render data map, identical to lines 412-420 in the /api/board handler. The second critique's suggestion to buffer template output via bytes.Buffer is a good defensive measure but is a separate hardening concern — it would prevent partial HTML on ANY template error, not just this one, and should be a follow-up improvement rather than gating the primary fix. The second critique is also correct that the collapsed-stage UX issue is unrelated and should not be conflated. Recommended fix: (1) Add the 4-line activeSlugs computation loop before line 252, (2) add '\"ActiveSlugs\": activeSlugs' to the data map at line 258. No other changes needed — the /api/board endpoint already works correctly, so once the initial render matches, the polling cycle will maintain correct state."
}
