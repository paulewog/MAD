{
  "summary": "The feature requires adding an 'ideation_prompt' field to the FeatureFile schema that gets injected into the ideating.md template as a top-level directive above the role instructions. Implementation touches four layers: (1) state.py - add ideation_prompt property to FeatureFile with empty string default, (2) prompts/ideating.md - add a conditional section like '{ideation_prompt_section}' that renders as a prominent directive block when non-empty, (3) phases.py run_ideating() - pass the new field into the prompt template, and (4) editing UIs - add the field to UnifiedEditModal in tui.py and the WebUI edit modal in client_full2.html plus a new edit-ideation-prompt API endpoint in api.go. The field should be editable from any stage (not just ideas) since the whole point is re-sending items through ideation with new direction, and it should be clearable so subsequent ideation rounds can run without the seed if desired."
}
